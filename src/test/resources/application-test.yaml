spring:
  r2dbc:
    url: r2dbc:h2:mem:///quotation;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

  flyway:
    locations: classpath:migration
    url: jdbc:h2:mem:quotation;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    user: sa
    password: sa